function square(x) { 
    var n = +x    
    return n * n 
} 

function area(r) { 
    r = +r; return Math.PI * r * r 
} 

function add1(x) { 
    return x + 1 
}

function area(r) { r = +r return Math.PI * r * r }